# Changelog

## 0.2.0

**改进**

- 审阅结束后有明确反馈：右上角显示一条说明。没发现问题时是「✓ 没有发现问题」+ “你的思路没有问题，…”并简单回顾这段知识；改了内容时是「✎ 已修改 N 处」+ 为什么改、正确的理解是什么。停留时间可在设置里调（0 = 一直显示，点击关闭）。
- 右键菜单改为一个「Note Proofreader」子菜单，包含「审阅修改」和「取消高亮」。
- 新增「取消高亮（选区）」：去掉选区内所有 `==高亮==` 标记，保留文字，一步撤销。

**修复的 0.1.0 问题**

- 双击 Alt 审阅后，如果内容没有错误，只有一次绿色闪烁，没有任何文字反馈，无法确认“检查过了且是对的”。
- 审阅加上的浅蓝高亮没有便捷的清除方式，只能手动删 `==`。

## 0.1.0

首个版本。

- 选中段落 → 右键「审阅修改」或双击 Alt，让模型只修正事实性错误，保留原话。
- 整段颗粒散开/聚合动画；改动处以 `==高亮==` 写入，显示为浅蓝。
- 支持 OpenAI 兼容接口（DeepSeek / Gemini / OpenRouter / 自定义）和 Anthropic Claude。
- 「演示动画（不调用 API）」命令。

已知问题（在 0.2.0 修复）：无错误时缺少文字反馈；高亮无法一键清除。
