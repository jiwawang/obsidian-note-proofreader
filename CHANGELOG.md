# Changelog

## 0.3.1

- 插件显示名改为 **Veritas Howler – Note Proofreader**；右键菜单里仍显示为 “Veritas Howler”。

## 0.3.0

- 插件显示名改为 **Veritas Howler**（插件 ID 仍是 `note-proofreader`，已安装的不受影响）。
- 需要 Obsidian 1.13.0 及以上：设置页改用新的声明式设置 API，设置项现在能在 Obsidian 的设置搜索里搜到。
- 反馈卡片改为画在编辑器的独立图层上，不再依赖覆盖 Obsidian 样式的 `!important`，也不会被正文裁剪。
- 颗粒动画期间隐藏文字的方式改为整段透明，修复了深色主题下聚合颗粒颜色偏黑的问题。
- 按 Obsidian 插件审核意见清理：改用 Obsidian 的 `createEl` 等 DOM 工具和 `window.requestAnimationFrame`，声明 CodeMirror 依赖，去掉 `builtin-modules`，补齐类型；发布时附带 GitHub 构建证明（artifact attestation）；README 改为英文为主。

## 0.2.3

- 修复：反馈卡片在 Obsidian 里不显示。Obsidian 对所有块级挂件加了 `contain: paint`，把悬浮在锚点下方的卡片整个裁掉了；现已对卡片锚点关闭该属性。

## 0.2.2

- 反馈改为悬浮在这段文字正下方的毛玻璃卡片：半透明底 + 背景模糊，配色跟随 Obsidian 主题（浅色主题黑字），右上角圆形 × 关闭；不再用右上角通知。
- 反馈内容改为老师式：开头直接给判断（不再有“你的思路没有问题”套话），对的指出抓到的关键点，错的说明错因和正确理解，最后视情况补一句前提、易混点或提醒。
- 设置和说明里注明 Mac 上双击 Option (⌥) 等同于 Alt。

## 0.2.1

- 修复：反馈通知里的文字颜色和通知底色相同，看起来一片黑。
- 没有问题时的反馈改为用模型自己的话**复述你写的内容**（保留你的用词和比喻），而不是泛泛地回顾知识点，方便确认它理解的和你想表达的一致。

## 0.2.0

**改进**

- 审阅结束后有明确反馈：右上角显示一条说明。没发现问题时是「✓ 没有发现问题」+ “你的思路没有问题，…”并简单回顾这段知识；改了内容时是「✎ 已修改 N 处」+ 为什么改、正确的理解是什么。停留时间可在设置里调（0 = 一直显示，点击关闭）。
- 右键菜单改为一个「Note Proofreader」子菜单，包含「审阅修改」和「取消高亮」。
- 新增「取消高亮（选区）」：去掉选区内所有 `==高亮==` 标记，保留文字，一步撤销。

**修复的 0.1.0 问题**

- 双击 Alt 审阅后，如果内容没有错误，只有一次绿色闪烁，没有任何文字反馈，无法确认“检查过了且是对的”。
- 审阅加上的浅蓝高亮没有便捷的清除方式，只能手动删 `==`。

## 0.1.0

首个版本。

- 选中段落 → 右键「审阅修改」或双击 Alt，让模型只修正事实性错误，保留原话。
- 整段颗粒散开/聚合动画；改动处以 `==高亮==` 写入，显示为浅蓝。
- 支持 OpenAI 兼容接口（DeepSeek / Gemini / OpenRouter / 自定义）和 Anthropic Claude。
- 「演示动画（不调用 API）」命令。

已知问题（在 0.2.0 修复）：无错误时缺少文字反馈；高亮无法一键清除。
